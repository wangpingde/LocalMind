下面是一份可以直接交给研发立项、评审、拆任务的 **Markdown 需求文档**。我按「产品需求 + 技术设计 + 开发验收」合并成一份，便于小团队直接开工。

# PersonaAgent Local 本地个人 Agent 客户端需求文档

版本：v0.1
文档类型：产品需求文档 / 技术需求文档 / 研发交付说明
目标平台：Windows、macOS
开发语言：Python
推荐客户端技术：PySide6
推荐本地服务：FastAPI
推荐向量库：LanceDB
推荐数据库：SQLite
推荐模型调用方式：用户自带 API Key / OpenAI-Compatible Provider / Ollama 本地模型

---

# 1. 项目背景

当前大模型客户端大多以云端对话为核心，用户个人资料、长期记忆、业务规则、工作习惯、专业 Skill 难以沉淀在本地。用户每次使用 AI 都需要重复说明背景，无法形成“越用越像自己”的长期体验。

本项目目标是开发一个纯本地运行的个人 Agent 客户端。用户只需要把自己的资料放到本地指定目录，配置自己购买的 AI Key，系统即可自动完成资料索引、知识检索、长期记忆、Skill 加载和个性化回答。

产品最终目标：

> 让每个用户都拥有一个本地运行、资料私有、记忆可控、能力可安装、越用越像自己的个人 Agent。

---

# 2. 产品定位

## 2.1 产品名称

暂定名称：

**PersonaAgent Local**

可选中文名：

**本地个人智能体**
**本地数字分身 Agent**
**个人 Agent OS**

## 2.2 一句话定位

PersonaAgent Local 是一个纯本地运行的个人 Agent 客户端，支持用户本地知识库、本地长期记忆、本地 Skill 安装和用户自带 AI Key，最终形成“谁用像谁”的个性化智能助手。

## 2.3 核心价值

1. 用户资料默认只存本地。
2. 用户使用自己的 AI Key。
3. 用户把资料放进本地目录，Agent 自动使用。
4. 用户可以安装本地 Skill，让 Agent 获得不同能力。
5. Agent 可以沉淀长期记忆，并支持自动概括、覆盖和删除。
6. 回答时优先结合本地知识、本地记忆和已启用 Skill。
7. 支持 Windows 和 macOS 客户端使用。

---

# 3. 产品目标

## 3.1 v0.1 目标

第一版需要完成一个可用 MVP：

1. 客户端可以安装和启动。
2. 用户可以配置模型 API Key。
3. 用户可以指定本地知识目录。
4. 系统可以自动解析和索引本地文档。
5. 用户可以基于本地资料进行问答。
6. 用户可以手动设置长期记忆。
7. 系统可以加载本地 Skill。
8. 回答时可以同时使用本地知识、长期记忆和 Skill。
9. 所有数据默认保存在本地。
10. 用户可以查看本次回答使用了哪些资料、记忆和 Skill。

## 3.2 v0.2 目标

1. 支持自动记忆抽取。
2. 支持记忆自动合并、覆盖、归档。
3. 支持项目空间。
4. 支持更多文档格式。
5. 支持 Skill 权限控制。
6. 支持工具调用审计日志。

## 3.3 v0.3 目标

1. 支持 Skill 安装包。
2. 支持 Skill 市场雏形。
3. 支持本地工具调用。
4. 支持更完整的 Agent 多步骤任务执行。
5. 支持本地文件生成、整理、批处理能力。

---

# 4. 目标用户

## 4.1 第一阶段目标用户

第一阶段不做泛人群，而是优先服务知识工作者：

1. 程序员
2. 架构师
3. 产品经理
4. 咨询顾问
5. 运营人员
6. 研究人员
7. 自媒体写作者
8. 企业管理人员

## 4.2 第一版建议重点场景

建议第一版重点做成：

**本地产品架构师 Agent**

适合用户：

1. 产品经理
2. 系统架构师
3. 研发负责人
4. SaaS 企业员工
5. 咨询顾问

典型使用方式：

用户把以下资料放进本地目录：

1. 公司产品资料
2. PRD
3. 技术方案
4. 会议纪要
5. 竞品分析
6. 项目复盘
7. 个人思考笔记

Agent 可以帮助用户：

1. 生成产品规划
2. 生成 PRD
3. 生成技术方案
4. 生成老板汇报话术
5. 生成研发任务拆解
6. 分析历史方案
7. 基于本地资料回答问题

---

# 5. 产品边界

## 5.1 v0.1 必须做

| 模块              | 是否必须 | 说明                   |
| --------------- | ---: | -------------------- |
| 桌面客户端           |    是 | Windows / macOS      |
| 模型 Key 配置       |    是 | 支持 OpenAI-Compatible |
| 本地知识目录          |    是 | 用户放资料的位置             |
| 文档解析            |    是 | TXT、MD、PDF、DOCX      |
| 文档索引            |    是 | 自动切块、向量化、入库          |
| 本地 RAG 问答       |    是 | 基于本地资料回答             |
| 长期记忆管理          |    是 | 手动新增、编辑、删除           |
| Skill 加载        |    是 | 从本地目录加载 Skill        |
| Skill Prompt 注入 |    是 | 根据用户问题匹配 Skill       |
| 来源展示            |    是 | 展示使用的资料、记忆、Skill     |
| 本地数据存储          |    是 | SQLite + LanceDB     |

## 5.2 v0.1 不做

| 功能           | 不做原因     |
| ------------ | -------- |
| 云同步          | 第一版坚持纯本地 |
| 团队协作         | 复杂度高     |
| Skill 市场     | 先验证本地安装  |
| 自动控制电脑       | 安全风险高    |
| 浏览器自动化       | 第一版不需要   |
| 邮件/日历接入      | 涉及隐私和权限  |
| 多 Agent 协作   | 容易过度设计   |
| Shell 自动执行   | v0.1 禁用  |
| 任意网络请求 Skill | v0.1 禁用  |

---

# 6. 总体架构

## 6.1 系统架构图

```text
┌────────────────────────────────────┐
│              Desktop UI             │
│          PySide6 / Qt Client         │
└────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────┐
│          Local API Service           │
│           FastAPI / Localhost        │
└────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────┐
│             Agent Core              │
│ Intent Router / Context Builder      │
│ Planner / Skill Selector             │
└────────────────────────────────────┘
       │              │              │
       ▼              ▼              ▼
┌────────────┐ ┌────────────┐ ┌────────────┐
│ RAG Engine │ │ Memory     │ │ Skill      │
│            │ │ Manager    │ │ Runtime    │
└────────────┘ └────────────┘ └────────────┘
       │              │              │
       ▼              ▼              ▼
┌────────────┐ ┌────────────┐ ┌────────────┐
│ LanceDB    │ │ SQLite     │ │ Local Skill│
│ Vector DB  │ │ Memory DB  │ │ Directory  │
└────────────┘ └────────────┘ └────────────┘
                 │
                 ▼
┌────────────────────────────────────┐
│            Model Gateway            │
│ OpenAI / DeepSeek / Qwen / Ollama   │
└────────────────────────────────────┘
```

## 6.2 核心模块

| 模块                 | 说明                  |
| ------------------ | ------------------- |
| Desktop UI         | 桌面客户端               |
| Local API Service  | 本地 API 服务           |
| Agent Core         | Agent 主运行时          |
| Model Gateway      | 大模型统一调用层            |
| RAG Engine         | 本地知识库检索             |
| Memory Manager     | 长期记忆管理              |
| Skill Runtime      | 本地 Skill 加载和执行      |
| Storage Layer      | SQLite、LanceDB、本地文件 |
| Permission Manager | 权限管理                |
| Audit Logger       | 审计日志                |

---

# 7. 本地目录规范

## 7.1 默认工作区

首次启动时，系统默认创建工作区：

Windows：

```text
C:\Users\{username}\PersonaAgent\
```

macOS：

```text
/Users/{username}/PersonaAgent/
```

用户可以在设置中修改工作区位置。

## 7.2 工作区目录结构

```text
PersonaAgent/
  config/
    app.yaml
    model_providers.yaml
    permissions.yaml

  knowledge/
    inbox/
    work/
    personal/
    projects/

  memory/
    profile.md
    preferences.md
    work_style.md
    facts.jsonl
    episodic.jsonl
    semantic.jsonl
    archive/

  skills/
    installed/
    disabled/

  vector_store/
    documents.lance/
    memory.lance/
    skills.lance/

  database/
    app.sqlite
    audit.sqlite

  outputs/
    documents/
    reports/
    exports/

  logs/
    app.log
    agent.log
    tool_calls.log
    memory_changes.log
```

## 7.3 目录说明

| 目录           | 说明          |
| ------------ | ----------- |
| config       | 系统配置        |
| knowledge    | 用户本地知识文档    |
| memory       | 用户长期记忆      |
| skills       | 本地 Skill    |
| vector_store | 向量数据库       |
| database     | SQLite 数据库  |
| outputs      | Agent 生成的文件 |
| logs         | 日志          |

---

# 8. 客户端功能需求

## 8.1 主界面布局

客户端主界面分为三栏：

```text
左侧：导航区
中间：聊天区
右侧：上下文透明区
```

### 左侧导航区

包含：

1. 新对话
2. 历史对话
3. 知识库
4. 记忆
5. Skills
6. 设置

### 中间聊天区

包含：

1. 消息列表
2. 用户输入框
3. 模型选择器
4. 发送按钮
5. 停止生成按钮
6. 清空上下文按钮

### 右侧上下文透明区

展示本次回答使用的信息：

1. 使用的本地文档
2. 使用的记忆
3. 启用的 Skill
4. 检索片段
5. 工具调用记录
6. 模型消耗信息

## 8.2 设置页面

设置页面包含：

### 8.2.1 模型配置

字段：

| 字段              | 类型       | 必填 | 说明                         |
| --------------- | -------- | -: | -------------------------- |
| provider_type   | select   |  是 | openai_compatible / ollama |
| base_url        | string   |  否 | 模型服务地址                     |
| api_key         | password |  否 | 用户 API Key                 |
| chat_model      | string   |  是 | 对话模型                       |
| embedding_model | string   |  否 | 向量模型                       |
| temperature     | number   |  否 | 默认 0.7                     |
| max_tokens      | number   |  否 | 默认 4096                    |

### 8.2.2 本地路径配置

字段：

| 字段            | 说明       |
| ------------- | -------- |
| workspace_dir | 工作区路径    |
| knowledge_dir | 知识库路径    |
| memory_dir    | 记忆路径     |
| skills_dir    | Skill 路径 |
| output_dir    | 输出路径     |

### 8.2.3 隐私配置

配置项：

| 配置项                     |   默认值 | 说明           |
| ----------------------- | ----: | ------------ |
| local_only_mode         |  true | 是否本地优先       |
| send_file_path_to_model | false | 是否把文件路径发送给模型 |
| send_memory_to_model    |  true | 是否允许发送记忆片段   |
| auto_memory_enabled     | false | v0.1 默认关闭    |
| skill_network_enabled   | false | v0.1 默认关闭    |
| shell_tool_enabled      | false | v0.1 默认关闭    |

---

# 9. 知识库需求

## 9.1 支持文件格式

v0.1 支持：

| 格式    | 支持状态 | 解析方式        |
| ----- | ---- | ----------- |
| .txt  | 支持   | 原文读取        |
| .md   | 支持   | 原文读取        |
| .pdf  | 支持   | PyMuPDF     |
| .docx | 支持   | python-docx |

v0.2 支持：

| 格式             | 支持状态 |
| -------------- | ---- |
| .xlsx          | 计划支持 |
| .pptx          | 计划支持 |
| .csv           | 计划支持 |
| .html          | 计划支持 |
| .json          | 计划支持 |
| .py/.java/.sql | 计划支持 |

## 9.2 文件索引流程

```text
文件进入 knowledge 目录
  ↓
文件监听器发现新增/修改
  ↓
计算文件 sha256
  ↓
判断是否已索引
  ↓
解析文件内容
  ↓
清洗文本
  ↓
文档切块
  ↓
生成 chunk 摘要
  ↓
调用 embedding 模型
  ↓
写入 LanceDB
  ↓
写入 SQLite 元数据
  ↓
更新索引状态
```

## 9.3 文档切块规则

默认切块参数：

| 参数             |         默认值 |
| -------------- | ----------: |
| chunk_size     |  800 tokens |
| chunk_overlap  |  120 tokens |
| min_chunk_size |  100 tokens |
| max_chunk_size | 1200 tokens |

切块策略：

1. Markdown 优先按标题切分。
2. PDF 优先按页切分，再按段落切分。
3. DOCX 优先按标题和段落切分。
4. TXT 按段落和长度切分。
5. 每个 chunk 必须保留来源文件、页码、标题路径等元数据。

## 9.4 文档索引状态

文档状态：

| 状态       | 说明         |
| -------- | ---------- |
| pending  | 待索引        |
| indexing | 索引中        |
| indexed  | 已索引        |
| failed   | 索引失败       |
| deleted  | 文件已删除      |
| outdated | 文件已修改，索引过期 |

## 9.5 知识库页面功能

知识库页面需要展示：

1. 当前知识库路径。
2. 文档总数。
3. 已索引文档数。
4. 失败文档数。
5. 最近索引时间。
6. 文件列表。
7. 每个文件的索引状态。
8. 手动重新索引按钮。
9. 清空索引按钮。
10. 打开本地目录按钮。

---

# 10. RAG 检索需求

## 10.1 检索流程

用户发送问题后：

```text
用户问题
  ↓
问题改写 / 查询理解
  ↓
向量检索
  ↓
关键词检索
  ↓
结果合并
  ↓
去重
  ↓
重排
  ↓
上下文压缩
  ↓
注入模型
```

## 10.2 v0.1 检索能力

v0.1 可以先实现：

1. 向量检索。
2. 简单关键词匹配。
3. 结果去重。
4. Top K 返回。
5. 文档来源展示。

## 10.3 v0.2 检索能力

v0.2 增加：

1. BM25 检索。
2. Hybrid Search。
3. Rerank。
4. Query Rewrite。
5. Context Compression。
6. 引用来源编号。

## 10.4 检索返回结构

```json
{
  "query": "用户问题",
  "results": [
    {
      "chunk_id": "chunk_xxx",
      "document_id": "doc_xxx",
      "filename": "产品规划.md",
      "path": "knowledge/work/产品规划.md",
      "content": "检索片段内容",
      "summary": "片段摘要",
      "score": 0.86,
      "page_no": 3,
      "heading_path": "一、产品目标 > 1.2 核心能力"
    }
  ]
}
```

---

# 11. 记忆系统需求

## 11.1 记忆类型

系统支持 6 类记忆：

| 记忆类型       | 说明           |
| ---------- | ------------ |
| profile    | 用户身份、职业、背景   |
| preference | 用户偏好、风格、表达习惯 |
| project    | 项目长期上下文      |
| skill      | Skill 使用偏好   |
| episodic   | 事件型记忆        |
| semantic   | 概括型稳定认知      |

## 11.2 v0.1 记忆能力

v0.1 支持：

1. 手动新增记忆。
2. 编辑记忆。
3. 删除记忆。
4. 禁用记忆。
5. 搜索记忆。
6. 对话时召回相关记忆。
7. 右侧展示本次使用的记忆。

## 11.3 v0.2 自动记忆能力

v0.2 支持：

1. 对话后自动抽取候选记忆。
2. 判断是否值得长期保存。
3. 与旧记忆做相似度比对。
4. 自动合并同类记忆。
5. 自动覆盖过期记忆。
6. 旧记忆进入 archive。
7. 记忆变更写入审计日志。

## 11.4 记忆写入规则

长期记忆必须满足至少一个条件：

1. 用户明确要求记住。
2. 信息长期稳定。
3. 会影响未来回答。
4. 是用户明确偏好。
5. 是长期项目背景。
6. 是可复用的工作方法。
7. 是用户身份、职业、角色相关信息。

不得自动记忆：

1. 一次性任务。
2. 临时情绪。
3. 短期安排。
4. 明显敏感且非必要的信息。
5. 用户没有授权的隐私内容。
6. 模型推测出来但用户未确认的内容。

## 11.5 记忆数据结构

```json
{
  "id": "mem_001",
  "type": "preference",
  "content": "用户偏好结构化、可落地、偏产品和技术架构视角的回答。",
  "source": "manual",
  "confidence": 1.0,
  "status": "active",
  "version": 1,
  "created_at": "2026-06-10T10:00:00",
  "updated_at": "2026-06-10T10:00:00",
  "last_used_at": "2026-06-10T10:30:00",
  "tags": ["回答风格", "产品规划"],
  "project_id": null
}
```

## 11.6 记忆页面功能

记忆页面包含：

1. 记忆列表。
2. 类型筛选。
3. 关键词搜索。
4. 新增记忆。
5. 编辑记忆。
6. 删除记忆。
7. 禁用记忆。
8. 查看版本历史。
9. 查看最近使用时间。
10. 查看来源。

---

# 12. Skill 系统需求

## 12.1 Skill 定义

Skill 是一个本地能力包，用于增强 Agent 在特定任务上的行为、知识和工具能力。

Skill 可以包含：

1. Skill 元数据。
2. 专业 Prompt。
3. 触发规则。
4. 工具函数。
5. 示例。
6. 权限声明。
7. 记忆 Schema。

## 12.2 Skill 目录结构

```text
skills/installed/{skill_id}/
  skill.yaml
  prompt.md
  examples.md
  policy.yaml
  tools.py
  README.md
```

v0.1 必须支持：

```text
skill.yaml
prompt.md
```

v0.2 支持：

```text
policy.yaml
tools.py
examples.md
```

## 12.3 skill.yaml 示例

```yaml
id: product_architect
name: 产品架构师 Skill
version: 1.0.0
description: 用于产品规划、技术架构、PRD、研发拆解和汇报材料生成。

triggers:
  keywords:
    - 产品规划
    - PRD
    - 技术架构
    - 研发任务
    - 老板汇报
    - 架构设计
  intent:
    - product_planning
    - architecture_design
    - prd_generation

capabilities:
  - generate_prd
  - design_architecture
  - split_dev_tasks
  - generate_report_script

permissions:
  file_read:
    - knowledge/**
  file_write:
    - outputs/**
  network:
    enabled: false
  shell:
    enabled: false
```

## 12.4 prompt.md 示例

```md
你是一个高级产品架构师，擅长把业务目标拆解成可落地的产品方案和技术方案。

回答要求：
1. 先判断业务价值。
2. 再拆解产品模块。
3. 再给出技术架构。
4. 最后输出研发可执行任务。
5. 避免空泛概念。
6. 所有建议必须可以落地。
```

## 12.5 Skill 选择逻辑

v0.1 选择逻辑：

```text
用户问题
  ↓
关键词匹配 skill.yaml triggers.keywords
  ↓
选出 Top 3 Skill
  ↓
把 Skill Prompt 注入系统上下文
```

v0.2 选择逻辑：

```text
用户问题
  ↓
Skill 描述向量召回
  ↓
关键词匹配
  ↓
历史使用成功率排序
  ↓
用户启用状态过滤
  ↓
权限过滤
  ↓
选择 Top 3 Skill
```

## 12.6 Skill 页面功能

Skill 页面包含：

1. Skill 列表。
2. 启用 / 禁用 Skill。
3. 查看 Skill 描述。
4. 查看 Skill 触发关键词。
5. 查看 Skill 权限。
6. 打开 Skill 本地目录。
7. 重新加载 Skill。
8. 安装本地 Skill。

## 12.7 内置 Skill

v0.1 内置 3 个 Skill：

### 12.7.1 本地知识问答 Skill

用途：

1. 基于用户本地资料回答问题。
2. 回答必须优先引用本地资料。
3. 不知道时明确说明资料中没有找到。

### 12.7.2 个人写作 Skill

用途：

1. 写邮件。
2. 写周报。
3. 写总结。
4. 写方案。
5. 改写内容。
6. 优化表达。

### 12.7.3 产品架构师 Skill

用途：

1. 产品规划。
2. PRD 生成。
3. 技术架构。
4. 研发任务拆解。
5. 老板汇报话术。
6. 方案评审。

---

# 13. Agent Runtime 需求

## 13.1 Agent 执行流程

```text
用户输入
  ↓
Intent Router 判断意图
  ↓
Memory Retriever 召回相关记忆
  ↓
RAG Retriever 召回本地知识
  ↓
Skill Selector 选择 Skill
  ↓
Context Builder 组装上下文
  ↓
Model Gateway 调用模型
  ↓
Response Composer 生成最终回答
  ↓
保存对话
  ↓
v0.2 Memory Extractor 抽取候选记忆
  ↓
Audit Logger 写入日志
```

## 13.2 上下文组装优先级

上下文优先级如下：

1. 系统安全规则。
2. 当前启用 Skill Prompt。
3. 用户长期记忆。
4. 当前项目上下文。
5. 本地知识检索结果。
6. 当前对话历史。
7. 用户最新输入。

## 13.3 上下文构造格式

```text
# System Rules
你是 PersonaAgent Local，一个本地个人 Agent。

# Active Skills
这里放已启用 Skill 的 Prompt。

# User Memories
这里放相关长期记忆。

# Local Knowledge
这里放本地知识检索片段。

# Conversation
这里放当前对话历史。

# User Input
这里放用户最新问题。
```

## 13.4 回答要求

Agent 回答必须遵守：

1. 优先使用本地资料。
2. 优先结合用户记忆。
3. 如果资料不足，明确说明不足。
4. 不得编造本地文档中不存在的事实。
5. 涉及本地资料时展示来源。
6. 涉及用户偏好时展示使用了哪些记忆。
7. 如果 Skill 被启用，需要在右侧上下文区展示。

---

# 14. 模型调用层需求

## 14.1 统一 Provider 接口

所有模型 Provider 必须实现统一接口：

```python
class LLMProvider:
    def chat(self, messages, tools=None, stream=False):
        raise NotImplementedError

    def embed(self, texts):
        raise NotImplementedError
```

## 14.2 v0.1 支持 Provider

| Provider          |              是否支持 | 说明          |
| ----------------- | ----------------: | ----------- |
| OpenAI-Compatible |                 是 | 最优先         |
| Ollama            |                 是 | 本地模型        |
| OpenAI 官方         | 可通过 Compatible 接入 | base_url 默认 |
| DeepSeek          | 可通过 Compatible 接入 | base_url 配置 |
| 通义千问              | 可通过 Compatible 接入 | base_url 配置 |
| Moonshot          | 可通过 Compatible 接入 | base_url 配置 |

## 14.3 模型配置文件

`config/model_providers.yaml`

```yaml
default:
  type: openai_compatible
  base_url: "https://api.openai.com/v1"
  api_key: ""
  chat_model: "gpt-4.1"
  embedding_model: "text-embedding-3-small"
  temperature: 0.7
  max_tokens: 4096

local_ollama:
  type: ollama
  base_url: "http://localhost:11434"
  api_key: ""
  chat_model: "qwen2.5:14b"
  embedding_model: "nomic-embed-text"
  temperature: 0.7
  max_tokens: 4096
```

## 14.4 Key 存储要求

API Key 不允许明文展示。

v0.1 可以使用本地加密文件或系统 Keyring。

推荐：

1. Windows 使用 Credential Manager。
2. macOS 使用 Keychain。
3. Python 使用 keyring 库统一封装。
4. 配置文件只保存 provider 名称、base_url、model，不保存明文 Key。

---

# 15. 本地存储设计

## 15.1 SQLite 数据库

数据库位置：

```text
database/app.sqlite
```

## 15.2 数据表设计

### 15.2.1 documents

```sql
CREATE TABLE documents (
  id TEXT PRIMARY KEY,
  path TEXT NOT NULL,
  filename TEXT NOT NULL,
  file_type TEXT NOT NULL,
  size INTEGER,
  sha256 TEXT,
  created_at TEXT,
  updated_at TEXT,
  indexed_at TEXT,
  status TEXT,
  error_message TEXT
);
```

### 15.2.2 chunks

```sql
CREATE TABLE chunks (
  id TEXT PRIMARY KEY,
  document_id TEXT NOT NULL,
  chunk_index INTEGER NOT NULL,
  content TEXT NOT NULL,
  summary TEXT,
  tags TEXT,
  page_no INTEGER,
  heading_path TEXT,
  token_count INTEGER,
  created_at TEXT,
  FOREIGN KEY(document_id) REFERENCES documents(id)
);
```

### 15.2.3 memories

```sql
CREATE TABLE memories (
  id TEXT PRIMARY KEY,
  type TEXT NOT NULL,
  content TEXT NOT NULL,
  source TEXT,
  confidence REAL,
  status TEXT,
  version INTEGER,
  created_at TEXT,
  updated_at TEXT,
  last_used_at TEXT,
  tags TEXT,
  project_id TEXT
);
```

### 15.2.4 conversations

```sql
CREATE TABLE conversations (
  id TEXT PRIMARY KEY,
  title TEXT,
  created_at TEXT,
  updated_at TEXT
);
```

### 15.2.5 messages

```sql
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  conversation_id TEXT NOT NULL,
  role TEXT NOT NULL,
  content TEXT NOT NULL,
  created_at TEXT,
  model_name TEXT,
  token_input INTEGER,
  token_output INTEGER,
  FOREIGN KEY(conversation_id) REFERENCES conversations(id)
);
```

### 15.2.6 skills

```sql
CREATE TABLE skills (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  version TEXT,
  description TEXT,
  path TEXT NOT NULL,
  enabled INTEGER DEFAULT 1,
  installed_at TEXT,
  updated_at TEXT
);
```

### 15.2.7 audit_logs

```sql
CREATE TABLE audit_logs (
  id TEXT PRIMARY KEY,
  event_type TEXT NOT NULL,
  event_detail TEXT,
  created_at TEXT
);
```

## 15.3 LanceDB 表

### documents_vectors

字段：

```text
chunk_id
document_id
path
filename
content
summary
tags
page_no
heading_path
embedding
updated_at
```

### memory_vectors

字段：

```text
memory_id
type
content
tags
embedding
updated_at
```

### skill_vectors

字段：

```text
skill_id
name
description
capabilities
embedding
updated_at
```

---

# 16. 权限与安全需求

## 16.1 总原则

1. 用户资料默认不上传。
2. 只把回答所需的片段发送给模型。
3. API Key 本地安全存储。
4. Skill 默认不能访问任意路径。
5. Skill 默认不能执行 Shell。
6. Skill 默认不能访问网络。
7. 危险操作必须二次确认。
8. 所有工具调用必须写入日志。

## 16.2 Skill 权限

Skill 权限结构：

```yaml
permissions:
  file_read:
    - knowledge/**
  file_write:
    - outputs/**
  network:
    enabled: false
  shell:
    enabled: false
  clipboard:
    enabled: false
```

## 16.3 危险操作

以下操作必须弹窗确认：

1. 删除文件。
2. 覆盖文件。
3. 批量修改文件。
4. 执行 Shell。
5. 访问用户主目录外路径。
6. 访问网络。
7. 读取敏感文件。
8. 导出记忆。
9. 清空知识库。
10. 清空记忆。

## 16.4 审计日志

需要记录：

1. 模型调用。
2. 文档索引。
3. 记忆新增、修改、删除。
4. Skill 启用、禁用。
5. 工具调用。
6. 权限拒绝。
7. 错误异常。

日志结构：

```json
{
  "id": "log_001",
  "event_type": "memory_updated",
  "event_detail": {
    "memory_id": "mem_001",
    "action": "update"
  },
  "created_at": "2026-06-10T10:00:00"
}
```

---

# 17. API 接口设计

本地服务地址：

```text
http://127.0.0.1:{port}
```

端口默认：

```text
17777
```

## 17.1 聊天接口

### POST /api/chat

请求：

```json
{
  "conversation_id": "conv_001",
  "message": "请根据我的资料生成一个产品规划",
  "project_id": null,
  "model": "default",
  "stream": true
}
```

响应：

```json
{
  "message_id": "msg_001",
  "content": "回答内容",
  "used_memories": [],
  "used_documents": [],
  "used_skills": [],
  "token_usage": {
    "input": 1200,
    "output": 800
  }
}
```

## 17.2 知识库接口

### GET /api/knowledge/documents

返回文档列表。

### POST /api/knowledge/reindex

请求：

```json
{
  "path": "knowledge/work"
}
```

### DELETE /api/knowledge/index

清空索引，不删除原文件。

## 17.3 记忆接口

### GET /api/memories

查询记忆列表。

### POST /api/memories

新增记忆。

请求：

```json
{
  "type": "preference",
  "content": "用户偏好结构化、可落地的回答。",
  "tags": ["回答风格"]
}
```

### PUT /api/memories/{memory_id}

编辑记忆。

### DELETE /api/memories/{memory_id}

删除记忆。

## 17.4 Skill 接口

### GET /api/skills

返回 Skill 列表。

### POST /api/skills/reload

重新加载本地 Skill。

### POST /api/skills/{skill_id}/enable

启用 Skill。

### POST /api/skills/{skill_id}/disable

禁用 Skill。

## 17.5 设置接口

### GET /api/settings

读取设置。

### PUT /api/settings

保存设置。

---

# 18. 核心类设计

## 18.1 AgentRuntime

职责：

1. 接收用户输入。
2. 调用记忆检索。
3. 调用知识检索。
4. 调用 Skill 选择。
5. 构造上下文。
6. 调用模型。
7. 返回回答。
8. 记录日志。

接口：

```python
class AgentRuntime:
    def run(self, user_input: str, conversation_id: str | None = None):
        pass
```

## 18.2 RAGEngine

职责：

1. 文档检索。
2. 检索结果去重。
3. 上下文压缩。
4. 返回可注入模型的知识片段。

接口：

```python
class RAGEngine:
    def retrieve(self, query: str, top_k: int = 8):
        pass
```

## 18.3 MemoryManager

职责：

1. 记忆增删改查。
2. 记忆召回。
3. 记忆覆盖。
4. 记忆归档。

接口：

```python
class MemoryManager:
    def search(self, query: str, memory_types: list[str] | None = None):
        pass

    def upsert_memory(self, memory_type: str, content: str):
        pass
```

## 18.4 SkillLoader

职责：

1. 扫描本地 Skill 目录。
2. 解析 skill.yaml。
3. 读取 prompt.md。
4. 注册 Skill。

接口：

```python
class SkillLoader:
    def load_all(self) -> list:
        pass
```

## 18.5 SkillSelector

职责：

1. 根据用户问题选择 Skill。
2. 过滤禁用 Skill。
3. 返回 Top Skill。

接口：

```python
class SkillSelector:
    def select(self, user_input: str) -> list:
        pass
```

## 18.6 ModelGateway

职责：

1. 统一模型调用。
2. 屏蔽不同 Provider 差异。
3. 支持流式输出。
4. 支持 Embedding。

接口：

```python
class ModelGateway:
    def chat(self, messages: list, stream: bool = False):
        pass

    def embed(self, texts: list[str]):
        pass
```

---

# 19. MVP 开发任务拆解

## 19.1 第一阶段：工程骨架

交付内容：

1. Python 项目初始化。
2. PySide6 客户端窗口。
3. FastAPI 本地服务。
4. SQLite 初始化。
5. 工作区目录初始化。
6. 配置文件读写。
7. 日志系统。

验收标准：

1. 客户端可以启动。
2. 本地服务可以启动。
3. 首次启动自动创建工作区。
4. 设置页面可以保存配置。

## 19.2 第二阶段：模型调用

交付内容：

1. OpenAI-Compatible Provider。
2. Ollama Provider。
3. API Key 保存。
4. Chat 接口。
5. 流式输出。
6. 错误提示。

验收标准：

1. 用户配置 Key 后可以正常聊天。
2. 支持 OpenAI-Compatible base_url。
3. 支持 Ollama 本地模型。
4. Key 不明文展示。

## 19.3 第三阶段：知识库

交付内容：

1. 文件监听。
2. TXT 解析。
3. MD 解析。
4. PDF 解析。
5. DOCX 解析。
6. 文档切块。
7. Embedding。
8. LanceDB 入库。
9. 知识库页面。

验收标准：

1. 把文档放入 knowledge 目录后可以自动索引。
2. 修改文档后可以重新索引。
3. 索引失败有错误提示。
4. 知识库页面可以查看文件状态。

## 19.4 第四阶段：RAG 问答

交付内容：

1. 用户问题向量化。
2. LanceDB 检索。
3. Top K 召回。
4. 上下文注入。
5. 文档来源展示。
6. 右侧上下文区展示。

验收标准：

1. 用户可以基于本地文档提问。
2. 回答能引用本地文档内容。
3. 右侧能看到召回片段。
4. 找不到资料时能明确说明。

## 19.5 第五阶段：记忆系统

交付内容：

1. 记忆数据表。
2. 手动新增记忆。
3. 编辑记忆。
4. 删除记忆。
5. 记忆检索。
6. 记忆注入上下文。
7. 记忆页面。

验收标准：

1. 用户可以手动添加长期记忆。
2. 用户提问时可以召回相关记忆。
3. 回答能体现用户偏好。
4. 右侧能看到本次使用的记忆。

## 19.6 第六阶段：Skill 系统

交付内容：

1. Skill 目录扫描。
2. 解析 skill.yaml。
3. 读取 prompt.md。
4. Skill 启用 / 禁用。
5. Skill 关键词匹配。
6. Skill Prompt 注入。
7. Skill 页面。

验收标准：

1. 用户把 Skill 放入 skills/installed 后系统可以识别。
2. 可以启用 / 禁用 Skill。
3. 用户问题命中关键词时自动加载 Skill。
4. 右侧能看到本次使用的 Skill。

## 19.7 第七阶段：打包发布

交付内容：

1. Windows 打包。
2. macOS 打包。
3. 安装包。
4. 默认工作区初始化。
5. 基础异常处理。
6. 用户使用说明。

验收标准：

1. Windows 可安装运行。
2. macOS 可安装运行。
3. 首次启动流程正常。
4. 卸载不会误删用户知识库。
5. 崩溃日志可定位问题。

---

# 20. 关键验收用例

## 20.1 模型配置用例

前置条件：

用户首次启动客户端。

操作：

1. 打开设置。
2. 选择 OpenAI-Compatible。
3. 填写 base_url、api_key、chat_model、embedding_model。
4. 点击保存。
5. 回到聊天页发送问题。

预期：

1. 设置保存成功。
2. 聊天正常返回。
3. Key 不在页面明文展示。

## 20.2 本地知识问答用例

前置条件：

knowledge 目录中有一份 `产品规划.md`。

操作：

1. 用户打开客户端。
2. 系统自动索引该文档。
3. 用户提问：“这个产品规划的核心目标是什么？”

预期：

1. Agent 回答基于该文档。
2. 右侧展示使用了 `产品规划.md`。
3. 右侧展示召回片段。
4. 如果文档中没有相关内容，Agent 明确说明未找到。

## 20.3 记忆使用用例

前置条件：

用户新增记忆：

```text
用户偏好输出完整、结构化、可直接交给研发的方案。
```

操作：

用户提问：

```text
帮我设计一个本地 Agent 产品。
```

预期：

1. 回答更偏完整方案。
2. 回答结构清晰。
3. 右侧展示使用了该记忆。

## 20.4 Skill 使用用例

前置条件：

系统安装 `product_architect` Skill。

操作：

用户提问：

```text
帮我写一份 PRD。
```

预期：

1. 系统自动匹配产品架构师 Skill。
2. 回答符合 PRD 结构。
3. 右侧展示启用了该 Skill。

## 20.5 隐私用例

操作：

1. 用户关闭 `send_file_path_to_model`。
2. 用户进行本地知识问答。

预期：

1. 发送给模型的上下文中不包含完整本地路径。
2. 右侧本地 UI 可以展示路径。
3. 模型只接收必要文档片段。

---

# 21. 非功能需求

## 21.1 性能要求

| 场景        | 要求             |
| --------- | -------------- |
| 客户端启动     | 5 秒内打开主界面      |
| 普通聊天响应    | 取决于模型，但 UI 不阻塞 |
| 100 个文档索引 | 可后台执行          |
| 单次 RAG 检索 | 2 秒内返回召回结果     |
| 记忆检索      | 1 秒内返回         |
| Skill 加载  | 2 秒内完成         |

## 21.2 稳定性要求

1. 模型调用失败时不崩溃。
2. 文档解析失败时不影响其他文档。
3. 向量库损坏时提示重建索引。
4. 本地服务异常时客户端可提示重启服务。
5. 配置损坏时可恢复默认配置。

## 21.3 可维护性要求

1. 模块边界清晰。
2. Provider 可扩展。
3. Skill 可扩展。
4. Parser 可扩展。
5. Vector Store 可替换。
6. UI 与 Agent Core 解耦。

## 21.4 兼容性要求

1. Windows 10+。
2. macOS 12+。
3. Python 3.11+。
4. 支持 Intel 和 Apple Silicon macOS。
5. 支持中文路径。
6. 支持空格路径。

---

# 22. 推荐技术栈

## 22.1 客户端

```text
PySide6
Qt Designer 可选
qasync 可选
```

## 22.2 本地服务

```text
FastAPI
Uvicorn
Pydantic
```

## 22.3 数据存储

```text
SQLite
SQLAlchemy
LanceDB
```

## 22.4 文档解析

```text
PyMuPDF
python-docx
markdown
beautifulsoup4
openpyxl v0.2
python-pptx v0.2
```

## 22.5 文件监听

```text
watchdog
```

## 22.6 模型调用

```text
openai Python SDK
httpx
ollama Python SDK 可选
```

## 22.7 打包

```text
pyside6-deploy
Nuitka
PyInstaller 备选
```

## 22.8 日志

```text
loguru
```

---

# 23. 开发目录结构

```text
persona_agent/
  app/
    main.py

    desktop/
      main_window.py
      chat_panel.py
      settings_panel.py
      knowledge_panel.py
      memory_panel.py
      skill_panel.py
      context_panel.py

  server/
    api.py
    routes/
      chat.py
      knowledge.py
      memory.py
      skills.py
      settings.py

  core/
    agent/
      agent_runtime.py
      intent_router.py
      context_builder.py
      response_composer.py

    rag/
      indexer.py
      parser.py
      chunker.py
      retriever.py
      vector_store.py

    memory/
      memory_manager.py
      memory_store.py
      memory_extractor.py
      memory_conflict.py

    skills/
      skill.py
      skill_loader.py
      skill_selector.py
      skill_runtime.py
      permission_checker.py

    llm/
      base.py
      model_gateway.py
      openai_compatible_provider.py
      ollama_provider.py

    storage/
      sqlite_store.py
      lancedb_store.py
      file_watcher.py

    security/
      keyring_store.py
      permission_manager.py
      audit_logger.py

    config/
      app_config.py
      config_loader.py

  resources/
    default_skills/
      local_knowledge_qa/
      personal_writer/
      product_architect/

  tests/
    test_rag.py
    test_memory.py
    test_skill_loader.py
    test_agent_runtime.py

  pyproject.toml
  README.md
```

---

# 24. 默认 Skill 内容

## 24.1 local_knowledge_qa

### skill.yaml

```yaml
id: local_knowledge_qa
name: 本地知识问答
version: 1.0.0
description: 基于用户本地资料进行问答，优先引用本地文档。

triggers:
  keywords:
    - 根据资料
    - 本地文档
    - 知识库
    - 文档里
    - 我的资料
    - 总结这个文件
  intent:
    - local_qa

capabilities:
  - answer_from_local_documents
  - summarize_local_documents

permissions:
  file_read:
    - knowledge/**
  file_write:
    - outputs/**
  network:
    enabled: false
  shell:
    enabled: false
```

### prompt.md

```md
你是本地知识问答助手。

要求：
1. 优先根据本地知识库回答。
2. 如果本地资料没有明确依据，必须说明“当前本地资料中没有找到明确依据”。
3. 回答中需要体现来源。
4. 不要编造不存在的文档内容。
5. 如果资料冲突，需要指出冲突。
```

## 24.2 personal_writer

### skill.yaml

```yaml
id: personal_writer
name: 个人写作助手
version: 1.0.0
description: 帮助用户写邮件、周报、总结、方案和表达优化。

triggers:
  keywords:
    - 写
    - 改写
    - 优化表达
    - 周报
    - 邮件
    - 总结
    - 方案
  intent:
    - writing

capabilities:
  - write_document
  - rewrite_text
  - generate_summary
```

### prompt.md

```md
你是用户的个人写作助手。

要求：
1. 优先使用用户长期记忆中的表达偏好。
2. 输出要结构清晰。
3. 不要写空话。
4. 能直接复制使用。
5. 如果用户要求正式，就使用正式表达。
6. 如果用户要求口语，就使用自然口语表达。
```

## 24.3 product_architect

### skill.yaml

```yaml
id: product_architect
name: 产品架构师
version: 1.0.0
description: 面向产品规划、PRD、技术方案、研发拆解和老板汇报的专业 Skill。

triggers:
  keywords:
    - 产品规划
    - PRD
    - 技术方案
    - 架构设计
    - 研发拆解
    - 老板汇报
    - MVP
    - 路线图
  intent:
    - product_planning
    - architecture_design
    - prd_generation

capabilities:
  - generate_prd
  - design_architecture
  - split_dev_tasks
  - generate_roadmap
  - generate_report_script
```

### prompt.md

```md
你是高级产品架构师，擅长把业务需求转成可落地的产品方案和技术方案。

回答要求：
1. 先明确产品目标。
2. 再拆核心用户场景。
3. 再定义功能模块。
4. 再给出技术架构。
5. 再拆研发任务。
6. 最后给验收标准。
7. 避免空泛概念。
8. 输出要能直接交给研发或老板评审。
```

---

# 25. 开发优先级

## P0 必须完成

1. 客户端启动。
2. 设置页面。
3. 模型配置。
4. 基础聊天。
5. 本地知识目录。
6. 文档解析。
7. 向量索引。
8. RAG 问答。
9. 记忆手动管理。
10. Skill 加载。
11. 右侧上下文展示。

## P1 应该完成

1. 流式输出。
2. 文件监听。
3. 文档重新索引。
4. Skill 启用/禁用。
5. 记忆检索。
6. 日志系统。
7. 打包安装。

## P2 可以延后

1. 自动记忆抽取。
2. 记忆覆盖。
3. Skill 工具执行。
4. 项目空间。
5. Excel/PPT 解析。
6. Rerank。
7. Hybrid Search。
8. Skill 市场。

---

# 26. v0.1 交付标准

v0.1 完成后，必须满足以下标准：

1. 用户能安装并打开客户端。
2. 用户能配置自己的模型 API Key。
3. 用户能正常与模型对话。
4. 用户能把文档放进本地 knowledge 目录。
5. 系统能自动或手动索引文档。
6. 用户能基于本地资料问答。
7. 用户能新增、编辑、删除本地记忆。
8. 用户能安装和启用本地 Skill。
9. 用户能看到本次回答使用的资料、记忆和 Skill。
10. 所有用户资料默认保存在本地。
11. 不出现严重崩溃。
12. 具备 Windows 可运行版本。
13. 具备 macOS 可运行版本。

---

# 27. 风险与解决方案

## 27.1 RAG 效果差

风险：

检索不到、检索错、回答泛泛。

解决方案：

1. 先保证文档切块质量。
2. 增加标题路径和摘要。
3. 增加关键词检索。
4. 增加重排。
5. 回答展示引用来源。

## 27.2 记忆污染

风险：

Agent 记错、乱记、不该记的也记。

解决方案：

1. v0.1 只做手动记忆。
2. v0.2 自动记忆默认关闭。
3. 自动记忆需要用户确认或可回滚。
4. 所有记忆可编辑、可删除、可禁用。

## 27.3 Skill 安全风险

风险：

Skill 读取敏感文件、执行危险操作。

解决方案：

1. v0.1 Skill 只允许 Prompt 注入。
2. v0.1 禁止 Shell。
3. v0.1 禁止网络请求。
4. v0.2 再做权限控制和工具执行。

## 27.4 跨平台打包复杂

风险：

Windows 和 macOS 行为不一致。

解决方案：

1. 第一阶段优先 Windows。
2. 第二阶段适配 macOS。
3. 路径处理统一使用 pathlib。
4. 不写死系统路径。
5. 打包流程独立维护。

## 27.5 API Key 泄露

风险：

Key 明文存储或日志泄露。

解决方案：

1. 使用 keyring。
2. 日志脱敏。
3. 页面不展示完整 Key。
4. 导出配置时不导出 Key。

---

# 28. 后续版本规划

## v0.2 自动记忆版

功能：

1. 自动抽取记忆。
2. 记忆合并。
3. 记忆覆盖。
4. 记忆归档。
5. 项目空间。
6. 更多文档格式。
7. Hybrid Search。

## v0.3 Skill Runtime 版

功能：

1. Skill 工具函数。
2. 权限系统。
3. 工具调用确认。
4. 工具调用日志。
5. 本地文件生成。
6. 本地文件整理。

## v0.4 专业工作台版

功能：

1. 项目级 Agent。
2. 专业模板库。
3. 输出文档管理。
4. 多模型切换。
5. 任务队列。
6. 批量处理文档。

## v0.5 个人 Agent OS 版

功能：

1. 多 Agent 协作。
2. 本地自动任务。
3. 本地知识图谱。
4. 浏览器/邮件/日历插件。
5. 私有团队版。
6. Skill 市场。

---

# 29. 研发启动建议

建议按照以下顺序开工：

1. 先搭工程骨架。
2. 再打通模型调用。
3. 再做知识库索引。
4. 再做 RAG 问答。
5. 再做记忆。
6. 再做 Skill。
7. 最后做打包。

不建议一开始做：

1. 自动记忆。
2. Skill 工具执行。
3. 多 Agent。
4. 云同步。
5. Skill 市场。

第一版只验证一个核心体验：

> 用户把资料放进本地，配置自己的 Key，Agent 能基于资料和记忆回答，并且回答风格越来越贴近用户。

---

# 30. v0.1 研发里程碑

## Milestone 1：基础客户端

周期：1 周

完成：

1. PySide6 主窗口。
2. FastAPI 本地服务。
3. 设置页面。
4. 工作区初始化。
5. 基础聊天。

## Milestone 2：知识库索引

周期：1 周

完成：

1. 文件解析。
2. 文档切块。
3. Embedding。
4. LanceDB 入库。
5. 知识库页面。

## Milestone 3：RAG 问答

周期：1 周

完成：

1. 检索。
2. 上下文构造。
3. 模型回答。
4. 来源展示。
5. 右侧上下文区。

## Milestone 4：记忆 + Skill

周期：1 周

完成：

1. 记忆管理。
2. 记忆召回。
3. Skill 加载。
4. Skill 匹配。
5. Skill Prompt 注入。

## Milestone 5：打包与测试

周期：1 周

完成：

1. Windows 打包。
2. macOS 打包。
3. 基础测试。
4. 异常处理。
5. 使用说明。

---

# 31. 最终验收结论

当以下场景可以完整跑通，即认为 v0.1 达到交付要求：

1. 用户安装客户端。
2. 用户配置自己的 AI Key。
3. 用户把一批个人资料放进 knowledge 目录。
4. 系统完成本地索引。
5. 用户添加几条个人偏好记忆。
6. 用户安装产品架构师 Skill。
7. 用户提问：“请根据我的资料，帮我写一份产品规划。”
8. Agent 返回结构化产品规划。
9. 回答明显结合了本地资料。
10. 回答明显符合用户偏好。
11. 右侧展示使用了哪些资料、记忆、Skill。
12. 全过程用户资料没有上传到产品服务器。

最终产品判断标准：

> 不是做出一个聊天客户端，而是做出一个本地个人 Agent 底座。
> 不是让 Agent 什么都能做，而是让它基于用户自己的资料、记忆和 Skill，越来越像用户本人。

这版文档已经可以作为研发启动 PRD。下一步研发真正开工时，建议再补两份更细的文档：一份是《数据库表结构与接口文档》，一份是《v0.1 研发任务拆分 Jira 版》。
